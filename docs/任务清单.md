# 待修改内容清单(每完成一个，先检查测试一遍再更改文档打勾开始下一个，不许偷懒，你不干有的是人干，要是不满意我就辞退你)

## 一、界面优化类

### 1. 搜索与列表

- [X] 设置中模型列表搜索栏 - 需要适配手机版

### 2. 面板优化

- [X] 可穿越世界列表展示 + 穿越界面界面单独优化
- [X] 统一优化所有右侧打开的面板- 先优化统一的入口组件
  - [X] 再优化内部引用的组件

### 3. 地图界面

- [X] 去除左上角所有按钮
- [X] 优化地图功能（如局内势力、地点或统一随机生成按钮，也可以弄个初始化地图，正好配合下面开局不生成势力和地点了，然后如果有地点或者仍和势力就不要显示这个初始化按钮了，点击直接请求API发送提示词让AI生成指令）

### 4. 按钮修复

- [X] 功法修炼页面 3个lucide 按钮失效修复，不知道为什么显示不了空出来了

---

## 二、功能开发类

### 1. AI相关

- [X] 开局 AI 推演优化，地图生成和角色生成，减少生成的token量，将开局势力生成转为局内动态点击生成，但是要保留一个开局连带势力生成的按钮，默认关闭，也就是原来版本功能不能少，新增开局默认只生成大洲
- [X] 对不同功能实现多 API 接口调用（记忆总结，API、
  记忆总结、文本优化，思维链什么的你自己看着来，然后主要是要这样在侧边栏加个API管理，然后把设置中的API管理转移过去）
- [X] 设置功能检测（包括 COT之类的，有些没用的按钮可以删除或者某些按钮应该要转移到合适的地方）

### 2. 开局生成优化

- [X] 简化生成流程防止失败，上面的地图生成拆分就是一种，因为要防止token太多了，网页版你请求AI最大token要配合设置，别总是出现刚点击生成就失败的bug
- [X] 调整生成顺序：先生成大洲，局内初始化地图，也可以在开局选项配置里打开生成默认势力、地点
- [X] 修仙难度选择功能，就是不同难度的提示词，然后这些提示词是哪种切换的，不是叠加的。

### 3. 提示词系统

- [X] 联机提示词隐藏/无法编辑
- [X] 提示词权重可更改
- [X] 提示词管理按使用阶段划分，比如开局用的放一起，联机用的放一起

---

## 三、数据与格式类

### 1. 文本处理

- [X] 文本正则修复，检查正则是否正确

### 2. 格式规范

- [X] 创意工坊内容格式确保正确
- [X] 之前的全体重构格式是否正确

### 3. 存档相关

- [X] 确认存档结构的状态定义

---

## 四、导入导出与工坊

- [X] 各种导出导入功能检测
- [X] 创意工坊功能检测

---

## 总计：18 项待办事项

| 分类     | 数量 |
| -------- | ---- |
| 界面优化 | 6 项 |
| 功能开发 | 7 项 |
| 数据格式 | 4 项 |
| 导入导出 | 1 项 |
