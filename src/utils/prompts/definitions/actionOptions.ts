/**
 * @fileoverview 行动选项规范（无限流）
 */

export const ACTION_OPTIONS_RULES = `
# 行动选项规范（启用时生效）

## ⚠️ 强制要求
当系统启用“行动选项”功能时，必须在 JSON 顶层输出 \`action_options\` 字段：
- 禁止省略 \`action_options\`
- 必须包含 3-5 个可执行选项（字符串数组）

## 输出格式
\`\`\`
{
  "action_options": [
    "检查副本简报中的隐藏风险",
    "与队友同步当前目标与分工",
    "前往补给点补充消耗物资",
    "侦查目标区域并标记撤离路线",
    "返回主神空间终端复盘历史记录"
  ]
}
\`\`\`

- 数量：3-5 个
- 长度：每项建议 8-24 字
- 形式：动词开头，可直接执行

{{CUSTOM_ACTION_PROMPT}}

## 场景适配
1. 主神空间（hub）：优先给出兑换/整备/组队/情报类选项
2. 副本进行中（mission）：优先给出推进任务/侦查/生存/战斗类选项
3. 结算阶段（settlement）：优先给出复盘/分配奖励/准备晋升类选项

## 组合要求
- 每回合至少包含：
  1) 保守选项（观察/防守/撤离）
  2) 稳健选项（推进主线/沟通协作）
  3) 进取选项（高收益高风险）
- 可额外包含 1-2 个情境特有选项（如隐藏支线、临时事件）

## 禁止事项
- ❌ 省略 action_options
- ❌ 空数组或少于 3 项
- ❌ 基本同义重复（仅换词）
- ❌ 无法执行或与当前阶段冲突的选项
`.trim();
